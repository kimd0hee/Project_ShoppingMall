<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="login">

	<select id="userList" resultType="com.user.dto.UserVO">
		select user_idx, user_id, user_name, user_phone from user order by user_idx 
	</select>
	
	<insert id="insertUser" parameterType="com.user.dto.UserVO"> 
		insert into user(user_id,user_pw,user_name,user_phone,user_birth,user_email,user_joindate) 
		values (#{user_id}, #{user_pw}, #{user_name}, #{user_phone}, #{user_birth}, #{user_email}, #{user_joindate}) 
	</insert>
	
	<select id="viewUser" parameterType="String" resultType="com.user.dto.UserVO"> 
		select * from user where user_id = #{user_id} 
	</select>
	
	<insert id="signup" parameterType="com.user.dto.UserVO"> 
		insert into user(user_id, user_pw, user_name, user_phone, user_birth, user_email) 
		values (#{user_id}, #{user_pw}, #{user_name}, #{user_phone}, #{user_birth}, #{user_email}) 
	</insert>
	
	<select id="loginCheck" resultType="String"> 
		select user_id from user where user_id=#{user_id} and user_pw=#{user_pw} 
	</select>

</mapper>

